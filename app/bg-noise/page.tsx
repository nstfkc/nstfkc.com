import { NoiseParameters } from "./components/Parameters";

const BackgroundNoise = () => {
  return (
    <div className="w-screen h-screen">
      <NoiseParameters />
    </div>
  );
};

export default BackgroundNoise;
